{
  // tsconfig paths fixture — comments are tolerated, like tsc allows
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@lib/*": ["./src/*"],
      "exact": ["./src/exact"]
    }
  }
}
